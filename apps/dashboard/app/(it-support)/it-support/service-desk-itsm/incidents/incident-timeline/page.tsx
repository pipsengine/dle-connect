import { IncidentsWorkspace } from '../../_components/IncidentsWorkspace';
export const metadata = { title: 'Incident Timeline' };
export default function Page() {
  return <IncidentsWorkspace mode="timeline" title="Incident Timeline" />;
}
