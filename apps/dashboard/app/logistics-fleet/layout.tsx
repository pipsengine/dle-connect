import type { ReactNode } from 'react';

export const metadata = {
  title: 'Logistics & Fleet',
};

export default function LogisticsFleetPortalLayout({ children }: { children: ReactNode }) {
  return children;
}
