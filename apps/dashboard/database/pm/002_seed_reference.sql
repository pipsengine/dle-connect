/* Minimal DLE PM reference seed data. Replace identity GUIDs through migration mapping during integration. */
-- Deliberately excludes production users and clients. Seeds should be environment-specific.
