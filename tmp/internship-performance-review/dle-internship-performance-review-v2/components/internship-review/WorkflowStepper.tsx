import {Check,Clock,CornerDownLeft,Minus} from 'lucide-react'; import {Approval} from '@/types/review';
export default function WorkflowStepper({items}:{items:Approval[]}){return <div className="workflow">{items.map((a,i)=><div className="wf" key={a.step}><div className={'wfIcon '+a.status.toLowerCase()}>{a.status==='Approved'?<Check/>:a.status==='Returned'?<CornerDownLeft/>:a.status==='Skipped'?<Minus/>:<Clock/>}</div><div><small>STEP {i+1}</small><b>{a.step}</b><span>{a.approver||'Automatically bypassed'}</span><em>{a.status}{a.at?' · '+a.at:''}</em></div>{i<items.length-1&&<div className="wfLine"/>}</div>)}</div>}
