import Link from 'next/link'; import {ClipboardCheck,Clock3,CheckCircle2,RotateCcw,Plus,Filter,Download,ArrowUpRight} from 'lucide-react'; import Shell from './Shell'; import Kpi from './KpiCard'; import StatusBadge from './StatusBadge'; import {reviews} from '@/lib/mock-data';
export default function Dashboard(){return <Shell><div className="page"><div className="pageHead"><div><span className="eyebrow">INTERNSHIP PERFORMANCE REVIEW</span><h1>Performance review dashboard</h1><p>Initiate, evaluate, approve and track one-year internship transition assessments.</p></div><Link href="/performance/internship-reviews/new" className="primary"><Plus size={17}/>Initiate review</Link></div><div className="kpis"><Kpi label="Open reviews" value="12" sub="Across all departments" Icon={ClipboardCheck}/><Kpi label="Awaiting approval" value="5" sub="2 require HR attention" Icon={Clock3}/><Kpi label="Approved this month" value="8" sub="67% recommended" Icon={CheckCircle2}/><Kpi label="Returned / action" value="2" sub="Needs follow-up" Icon={RotateCcw}/></div><section className="panel"><div className="panelHead"><div><h2>Review register</h2><p>All internship assessments and current workflow position.</p></div><div className="row"><button className="secondary"><Filter size={15}/>Filter</button><button className="secondary"><Download size={15}/>Export</button></div></div><div className="filters"><input placeholder="Search intern, employee code or department..."/><select><option>All departments</option><option>Information Technology</option><option>Procurement</option></select><select><option>All statuses</option><option>In Evaluation</option><option>Pending MD</option><option>Approved</option></select></div><div className="tableWrap"><table><thead><tr><th>Review ID</th><th>Intern</th><th>Department</th><th>Line manager</th><th>Due date</th><th>Status</th><th>Score</th><th></th></tr></thead><tbody>{reviews.map(r=><tr key={r.id}><td><b>{r.id}</b></td><td><div className="person"><span>{r.employee.name.split(' ').map(x=>x[0]).slice(0,2)}</span><div><b>{r.employee.name}</b><small>{r.employee.code}</small></div></div></td><td>{r.employee.department}</td><td>{r.supervisor}</td><td>{r.dueDate}</td><td><StatusBadge status={r.status}/></td><td><b>{r.overall?r.overall.toFixed(1):'—'}</b></td><td><Link className="view" href={'/performance/internship-reviews/'+r.id}>Open <ArrowUpRight size={14}/></Link></td></tr>)}</tbody></table></div></section></div></Shell>}
